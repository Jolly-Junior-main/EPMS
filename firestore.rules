rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal read and write access for multi-browser real-time synchronization
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
